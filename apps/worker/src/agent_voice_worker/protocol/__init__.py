"""Versioned event and command contract shared with `packages/protocol`.

This is a self-contained Python port: the TypeScript package is the source of
truth for the wire format, but nothing here imports it. Keep the two in sync
by hand when the contract changes.
"""
